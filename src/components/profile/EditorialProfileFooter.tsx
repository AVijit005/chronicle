export function EditorialProfileFooter() {
  return (
    <footer className="mt-12 border-t border-white/5 pt-8 text-center">
      <p className="font-display text-2xl tracking-tight">Your story never stops.</p>
      <p className="mt-2 text-sm text-muted-foreground">
        Every entry, every quiet rewatch, every late-night chapter — Chronicle remembers.
      </p>
    </footer>
  );
}
